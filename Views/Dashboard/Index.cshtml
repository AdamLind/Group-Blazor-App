@{
    ViewData["Title"] = "Dashboard - Combined";
}

<!-- Link the custom dashboard CSS file -->
<link rel="stylesheet" href="~/css/dashboard.css" />

<div class="container mt-5">
    <h1 class="display-2 text-center mb-5">Dashboard</h1>
    <div class="row">
        <div class="col-md-6">
            @await Html.PartialAsync("_BooksTablePartial")
        </div>
        <div class="col-md-6">
            @await Html.PartialAsync("_ReadingPartial")
        </div>
    </div>
</div>